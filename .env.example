# Slack app credentials
# Obtain these from your Slack App configuration
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=

# Google Sheets: the ID of the Sheet where feedback will be appended
# Example: for URL https://docs.google.com/spreadsheets/d/THIS_IS_THE_ID/edit -> use THIS_IS_THE_ID
FEEDBACK_SHEET_ID=

# Feedback: runtime mode for Google auth
# Options: "desktop_oauth" (InstalledAppFlow -> secrets/token.pickle),
#          "service_account" (use GOOGLE_SERVICE_ACCOUNT_JSON or GOOGLE_SERVICE_ACCOUNT_PATH)
FEEDBACK_AUTH_MODE=desktop_oauth

# If you later use a service account, you can provide credentials via env or file path
# GOOGLE_SERVICE_ACCOUNT_JSON='{"type":...}'  # not recommended to put raw JSON here in repo
# GOOGLE_SERVICE_ACCOUNT_PATH=/path/to/service-account.json

# Path to the local secrets directory containing credentials.json and token.pickle
# By default the repo uses ./secrets
FEEDBACK_SECRETS_PATH=./secrets

# Optional toggles
# If set to 1, the bot will include local fallback sources in responses (MVP hides them)
ALLOW_LOCAL_SYNC_STATE_FALLBACK=1
## Copy this file to `.env` and fill real values before running the bot
## Do NOT commit your real `.env` (contains secrets)

# Slack Configuration
SLACK_BOT_TOKEN=xoxb-your-token-here
SLACK_APP_TOKEN=xapp-your-token-here

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# File Search Store Configuration
# Use the same store name as in Handbook_MVP_File_Search/.env
FILE_SEARCH_STORE_NAMES=fileSearchStores/your-store-id-here

# Bot Configuration
BUFFER_SECONDS=3.5
